defmodule InfusionsoftTest do
  use ExUnit.Case, async: true

  # TODO create a mock for XML and REST APIs so that doctests and integration tests can be used.
  # Unfortunately with the way Infusionsoft does its token system, there's not a good way to
  # test against the real API, even isolating those tests with a tag.
end
